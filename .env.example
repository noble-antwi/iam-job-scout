# ========== JOB SEARCH APIs ==========

# JSearch API via RapidAPI (primary source)
# Sign up at: https://rapidapi.com/letscrape-6bRBa3QguO5/api/jsearch
RAPIDAPI_KEY=your_rapidapi_key

# Adzuna API (optional - 250 free requests/month)
# Sign up at: https://developer.adzuna.com/
ADZUNA_APP_ID=your_adzuna_app_id
ADZUNA_APP_KEY=your_adzuna_app_key

# RemoteOK API - No authentication required (always enabled)

# ========== GOOGLE CSE (optional, alternative search) ==========
GOOGLE_CSE_API_KEY=your_google_cse_api_key
GOOGLE_CSE_CX=your_custom_search_engine_id

# ========== ADMIN AUTHENTICATION ==========
# Admin Authentication (REQUIRED for production - change the default!)
ADMIN_PASSWORD=your_secure_admin_password

# API Token for cron-triggered endpoints (RECOMMENDED for production)
ADMIN_API_TOKEN=your_secure_api_token_for_cron

# Session Secret (REQUIRED for production - generates random if not set but sessions reset on restart)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=your_random_session_secret_key

# ========== DATABASE ==========
# Database (optional - defaults to SQLite)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# ========== EMAIL NOTIFICATIONS (optional) ==========
SENDGRID_API_KEY=your_sendgrid_key
FROM_EMAIL=noreply@yourdomain.com
TO_EMAIL=your@email.com
